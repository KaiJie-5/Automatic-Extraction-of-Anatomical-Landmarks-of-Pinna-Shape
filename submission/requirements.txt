# Versions verified in anthropometric_env (Python 3.10.20) on 2026-08-24.
# The installed torch build was 2.9.1+cu128; the public version is pinned here
# so an evaluator can select the CUDA build appropriate for its own platform.
ipykernel==7.2.0
k3d==2.17.0
matplotlib==3.10.9
numpy==2.2.6
scipy==1.15.3
torch==2.9.1
trimesh==4.12.2
