# Optional exact Point Transformer V3 research environment only.
# Follow PTV3_EXPERIMENT.md; do not use this as the first install command in a
# fresh environment. Install PyTorch 2.1.0 + CUDA 11.8 with conda, apply the
# compatibility pins, and build FlashAttention with --no-build-isolation first.
-r requirements.txt
numpy==1.26.4
setuptools==69.5.1
wheel==0.43.0
packaging==24.0
psutil==5.9.8
ninja==1.13.0
fsspec
addict==2.4.0
timm==0.9.16
spconv-cu118==2.3.8
torch-scatter==2.1.2
flash-attn==2.5.9.post1
